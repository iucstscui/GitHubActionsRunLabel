name: RunLabel

on:
  pull_request:
    types: [closed]
jobs:
  update_release_draft:
    runs-on: ubuntu-latest

    if: (github.event.pull_request.merged == true && github.event.label.name == 'runAction')

    steps:
      - run: echo "Hello World!"